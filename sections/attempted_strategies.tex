\section{尝试策略}

\subsection{策略一：上下文裁剪}

对历史对话进行裁剪，只保留与当前轮请求直接相关的内容。该策略可以降低 prefill token 数，从而减少 TTFT，但需要避免删掉影响任务理解的关键信息。

\subsection{策略二：稳定 Prompt 前缀}

将系统提示词、工具说明和固定格式模板放在稳定前缀中，减少每轮请求中前缀内容的变化。稳定前缀有利于 prefix KV cache 命中，适合多轮对话 Agent 的连续请求场景。

\subsection{策略三：会话级 KV Cache 复用}

为同一个会话维护可复用的 KV cache，使新一轮请求只对新增用户输入和必要增量上下文进行 prefill。该策略对降低多轮 TTFT 最直接，但需要管理 cache 生命周期、显存占用和会话淘汰策略。

\subsection{策略四：工具调用后置或异步化}

对于不影响首轮响应的工具调用，尝试后置或异步执行，先让模型返回初始响应或状态提示。该策略可以降低端到端 TTFT，但只适用于工具结果不是首 token 必需条件的场景。

\subsection{策略五：请求调度优化}

通过限制最大 batch 等待时间、区分短上下文和长上下文请求、为交互式请求设置更高优先级等方式降低排队时间。该策略主要缓解高并发下 TTFT 抖动。

设计了一个本地准入调度模拟器，用于在真实推理引擎测试之前验证服务层调度策略的可行性。模拟器将请求划分为规划、工具调用、工具结果整合、推理、最终回答等阶段，并建模输入长度、输出长度、单步最大处理令牌数、长输入分块预填充、并发请求数和键值缓存容量等关键因素。具体策略包括：直接放行作为无控制基线；总并发限制策略通过限制同时进入推理引擎的请求数量避免系统过载；短请求优先策略根据输入长度区分短请求和长请求，优先放行短请求，并限制长请求并发数量；阶段感知策略进一步结合智能体任务阶段，对规划和工具调用等交互性强的阶段赋予更高优先级，同时加入等待时间补偿机制，避免长请求长期被饿死。评估指标包括平均首个生成结果等待时间、尾部首个生成结果等待时间、请求完成尾延迟、排队时间、生成阶段平均令牌间隔、输出吞吐、请求吞吐、会话总完成时间、键值缓存峰值占用率，以及不同任务阶段和长短请求类别下的延迟分布。该方案的工程落地路径是先在测试客户端或服务封装层中加入总并发限制，再加入长请求识别与长请求并发控制，最后尝试基于输入结构、轮次状态或上下文长度近似识别智能体阶段，实现阶段感知准入。

\subsection{策略五：Tool call cache}

\subsection{策略六：GDN下system prompt和user prompt的区分}
策略：统计频次进行匹配，但system prompt如果在调度层做会多算一次decode，所以不能直接在调度层进行切分
